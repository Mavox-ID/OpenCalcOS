# Redirections are not globbed.
# bash:
# if run as "sh", they are not globbed, but
# if run as "bash", they are!
>z.lib
echo TEST >?.lib
echo 'z.lib:' `cat 'z.lib'`
echo '?.lib:' `cat '?.lib'`
rm 'z.lib' '?.lib'
