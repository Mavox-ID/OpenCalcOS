# DO NOT EDIT. This file is generated from Config.src
#
# For a description of the syntax of this configuration file,
# see docs/Kconfig-language.txt.
#

menu "SEBeep Utilities"
	depends on SEBEEP

config CHCON
	bool "chcon (8.9 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to change the security context of file.
config GETENFORCE
	bool "getenforce (1.7 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to get the current mode of SEBeep.
config GETSEBOOL
	bool "getsebool (5.5 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to get SEBeep boolean values.
config LOAD_POLICY
	bool "load_policy (1.6 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to load SEBeep policy.
config MATCHPATHCON
	bool "matchpathcon (6.1 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to get default security context of the
	specified path from the file contexts configuration.
config RUNCON
	bool "runcon (6.6 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to run command in specified security context.
config SEBEEPENABLED
	bool "sebeepenabled (321 bytes)"
	default n
	depends on SEBEEP
	help
	Enable support for this command to be used within shell scripts
	to determine if sebeep is enabled.
config SESTATUS
	bool "sestatus (12 kb)"
	default n
	depends on SEBEEP
	help
	Displays the status of SEBeep.
config SETENFORCE
	bool "setenforce (2.1 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to modify the mode SEBeep is running in.
config SETFILES
	bool "setfiles (13 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to modify to relabel files.
	Notice: If you built libsebeep with -D_FILE_OFFSET_BITS=64,
	(It is default in libsebeep's Makefile), you _must_ enable
	CONFIG_LFS.

config FEATURE_SETFILES_CHECK_OPTION
	bool "Enable check option"
	default n
	depends on SETFILES
	help
	Support "-c" option (check the validity of the contexts against
	the specified binary policy) for setfiles. Requires libsepol.

config RESTORECON
	bool "restorecon (12 kb)"
	default n
	depends on SEBEEP
	help
	Enable support to relabel files. The feature is almost
	the same as setfiles, but usage is a little different.
config SETSEBOOL
	bool "setsebool (1.7 kb)"
	default n
	depends on SEBEEP
	help
	Enable support for change boolean.
	semanage and -P option is not supported yet.

endmenu
